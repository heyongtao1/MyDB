#ifndef _CONFIG_H
#define _CONFIG_H

#include <iostream>
#include <vector>
using namespace std;

class DBConfig{
public:
    static void read_db_config(vector<string> db_arr)
    {
        
    }
    static void write_db_config(const string& db_name)
    {

    }
};

#endif
